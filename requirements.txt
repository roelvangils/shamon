# Python dependencies for Shamon web server (serve.py)
# Install with: pip install -r requirements.txt

# Using flexible version constraints to avoid conflicts with other packages
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
